services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nins-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3000
      MONGO_URI: ${MONGO_URI}
