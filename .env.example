# ─── App ───
NODE_ENV=development
PORT=3000

# ─── MongoDB ───
MONGO_URI=mongodb://root:secret@mongo:27017/nins?authSource=admin

# ─── Database Pool (optional — defaults shown) ───
# DB_POOL_SIZE=50
# DB_MIN_POOL_SIZE=10

# ─── JWT (add when auth is implemented) ───
# JWT_SECRET=change-me-to-a-strong-secret
# JWT_EXPIRES_IN=7d
